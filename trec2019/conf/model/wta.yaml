# @package _global_
model:
  name: wta
  tags:
    - wta
  # boosting
  k_inference_factor: 1.5
  weight_sparsity: 0.3
  normalize_weights: true
  boost_strength: 1.0
  boost_strength_factor: 0.85
  duty_cycle_period: 5
  # regularizer
  dropout: 0.0
  use_sparse_weights: true
