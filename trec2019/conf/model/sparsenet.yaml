# @package _global_
model:
  name: sparsenet
  tags:
   - sparsenet
  # boosting
  k_inference_factor: 1.5
  weight_sparsity: 0.3
  boost_strength: 1.5
  boost_strength_factor: 0.85
  # regularizer
  dropout: 0.0
  normalize_weights: true
  use_batch_norm: true
